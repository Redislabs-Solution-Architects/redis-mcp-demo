# Redis Connection Configuration
# These values come from your Redis Cloud database details

# Redis host (e.g., redis-12345.c1.us-east-1-2.ec2.cloud.redislabs.com)
REDIS_HOST=your-redis-endpoint.cloud.redislabs.com

# Redis port (usually provided with the endpoint, e.g., 12345)
REDIS_PORT=12345

# Redis username (default is "default" for Redis Cloud)
REDIS_USERNAME=default

# Redis password (found in Redis Cloud console under Security settings)
REDIS_PASSWORD=your-redis-password

# Redis Cloud API Credentials
# Generate these from Redis Cloud Console > Account Settings > API Keys

# API Key (starts with "rxk_")
REDIS_CLOUD_API_KEY=rxk_your_api_key_here

# API Secret Key
REDIS_CLOUD_API_SECRET=your_api_secret_here

# Optional: Database ID for specific operations
# REDIS_DATABASE_ID=12345678

# Optional: Subscription ID for management operations
# REDIS_SUBSCRIPTION_ID=87654321